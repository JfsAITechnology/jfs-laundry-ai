/* Customer portal: secure Supabase RPC orders + tracking */
let dbTenant = null;let orderSubmitting = false;const customerParams=new URLSearchParams(location.search);const customerTenantId=customerParams.get('id')||'JFS-LAUNDRY-DEMO-001';const customerOrderKey=`jfs_orders_${customerTenantId}`;
function applyResolvedCustomerIdentity(){const cfg=window.activeConfig||{};const name=cfg.name||'JFS Laundry AI',address=cfg.address||'Alamat laundry';const header=document.getElementById('display-store-name'),addressEl=document.getElementById('display-store-address'),wa=document.getElementById('btn-wa-direct');const displayName=/\s+laundry$/i.test(name)?name:`${name} Laundry`;if(header)header.textContent=displayName;if(addressEl)addressEl.textContent=address;if(wa)wa.href=cfg.waNumber?`https://wa.me/${cfg.waNumber}?text=${encodeURIComponent('Halo '+name+', saya ingin bertanya.')}`:'#';document.querySelectorAll('h2').forEach(el=>{if(el.textContent.trim()==='JFS Laundry AI Assistant')el.textContent=`JFS AI Assistant — ${displayName}`})}
async function resolveDbTenant(){if(!window.jfsDb)return null;const{data,error}=await jfsDb.rpc('jfs_public_tenant_catalog',{p_tenant_code:customerTenantId});if(!error&&data?.length)dbTenant=data[0];if(error)console.warn('Tenant resolve failed',error);return dbTenant}
async function syncPortalConfig(){const tenant=await resolveDbTenant();if(!tenant){activeConfig={name:'JFS Laundry AI',address:'Alamat laundry',waNumber:'',pricelist:{}};window.activeConfig=activeConfig;applyResolvedCustomerIdentity();document.dispatchEvent(new CustomEvent('jfs:portal-config-ready'));return activeConfig}activeConfig={name:tenant.business_name||'JFS Laundry AI',address:tenant.address||[tenant.city,tenant.province].filter(Boolean).join(', ')||'Alamat laundry',waNumber:tenant.whatsapp||tenant.phone||'',pricelist:{}};window.activeConfig=activeConfig;applyResolvedCustomerIdentity();document.dispatchEvent(new CustomEvent('jfs:portal-config-ready'));let products=[];try{const{data,error}=await jfsDb.rpc('jfs_public_products',{p_tenant_code:tenant.tenant_code});if(error)throw error;products=data||[]}catch(error){console.warn('Portal products sync failed',error)}const pricelist={};products.forEach(p=>{const key=String(p.name||'').trim().toLowerCase();if(key)pricelist[key]=Number(p.price||0)});activeConfig.pricelist=pricelist;window.activeConfig=activeConfig;window.jfsPortalProducts=products;const unitSelect=document.getElementById('order-unit');if(unitSelect){const allPerKg=products.filter(p=>Number(p.price)>0).every(p=>/kilogram|per\s*kg|\/\s*kg/i.test(`${p.name||''} ${p.description||''}`));if(products.length&&allPerKg){unitSelect.value='Kg';unitSelect.innerHTML='<option value="Kg">Kg</option>';unitSelect.disabled=true}else unitSelect.disabled=false}applyResolvedCustomerIdentity();document.dispatchEvent(new CustomEvent('jfs:portal-config-ready'));return activeConfig}
function ensurePaymentField(){const form=document.querySelector('form[onsubmit="sendOrder(event)"]');if(!form||document.getElementById('order-payment'))return;const wrap=document.createElement('div');wrap.innerHTML='<label class="block text-[11px] mb-1">Pembayaran</label><select id="order-payment" class="w-full bg-slate-900 border border-slate-700 rounded-lg p-2"><option value="cash">Bayar saat selesai / di toko</option><option value="transfer">Transfer bank</option><option value="qris">QRIS</option></select><p class="text-[9px] text-slate-500 mt-1">Pembayaran online dapat dikonfirmasi admin.</p>';document.getElementById('order-notes')?.parentElement?.before(wrap)}
function ensureTrackingTokenField(){const input=document.getElementById('track-id');if(!input||document.getElementById('track-token'))return;const wrap=document.createElement('div');wrap.className='mt-2';wrap.innerHTML='<input id="track-token" placeholder="Kode pelacakan" autocomplete="off" class="field w-full"><p class="helper mt-1">Kode ini diberikan saat order berhasil dibuat.</p>';input.parentElement?.after(wrap)}
async function createDbOrder(order){const tenant=await resolveDbTenant();if(!tenant)throw new Error('Tenant database tidak ditemukan');const{data,error}=await jfsDb.rpc('jfs_create_customer_order',{p_tenant_code:tenant.tenant_code,p_external_id:order.id,p_name:order.name,p_phone:order.phone,p_service:order.service,p_quantity:order.qty,p_unit:order.unit,p_delivery:order.delivery,p_address:order.address,p_pickup_time:order.pickupTime,p_notes:order.notes,p_payment_method:order.payment});if(error)throw error;if(!data?.tracking_token)throw new Error('Tracking token tidak diterima server');return data}
async function trackOrder(){const id=document.getElementById('track-id')?.value.trim().toUpperCase(),token=document.getElementById('track-token')?.value.trim();if(!id||!token){alert('Masukkan nomor order dan kode pelacakan.');return}window.location.href=`track.html?id=${encodeURIComponent(customerTenantId)}&order=${encodeURIComponent(id)}&token=${encodeURIComponent(token)}`}
function newOrderId(){if(window.crypto?.randomUUID)return 'ORD-'+crypto.randomUUID().replace(/-/g,'').slice(0,12).toUpperCase();return 'ORD-'+Date.now().toString(36).toUpperCase()+Math.random().toString(36).slice(2,6).toUpperCase()}
function calculateTotal(service,qty,unit){const price=Number((activeConfig.pricelist||{})[service]||0);if(!Number(qty)||!price||unit!=='Kg')return 0;return Math.round(Number(qty)*price)}
function showOrderSuccess(orderId,trackingToken){const form=document.querySelector('form[onsubmit="sendOrder(event)"]');if(!form)return;document.getElementById('customer-order-success')?.remove();const card=document.createElement('div');card.id='customer-order-success';card.className='order-result rounded-2xl p-4 mt-4';card.innerHTML=`<div class="flex items-start gap-3"><div class="text-2xl">✅</div><div class="min-w-0 flex-1"><h3 class="font-extrabold text-base">Order berhasil dikirim</h3><p class="text-sm mt-1">Simpan nomor order ini untuk melihat perkembangannya.</p><div class="mt-3 bg-white/70 rounded-xl p-3"><div class="text-xs opacity-80">NOMOR ORDER</div><div class="font-extrabold text-lg break-all">${orderId}</div><div class="text-xs mt-2 opacity-80">STATUS</div><div class="font-bold">Menunggu Jemput</div></div><a href="track.html?id=${encodeURIComponent(customerTenantId)}&order=${encodeURIComponent(orderId)}&token=${encodeURIComponent(trackingToken)}" class="block text-center mt-3 bg-emerald-600 text-white font-extrabold py-3 rounded-xl">📦 Lihat / Lacak Pesanan</a></div></div>`;form.insertAdjacentElement('afterend',card);card.scrollIntoView({behavior:'smooth',block:'center'})}
async function sendOrder(e){e.preventDefault();if(orderSubmitting)return;const form=e.target,button=form.querySelector('button[type="submit"]')||form.querySelector('button:last-of-type'),name=document.getElementById('order-name').value.trim(),phone=normalizePhone(document.getElementById('order-phone').value),service=document.getElementById('order-service').value,qty=Number(document.getElementById('order-qty').value),unit=document.getElementById('order-unit').value,delivery=selectedDelivery(),address=delivery==='pickup'?document.getElementById('order-address').value.trim():'Antar sendiri',pickupTime=delivery==='pickup'?document.getElementById('pickup-time').value:'-',notes=document.getElementById('order-notes').value.trim()||'-',payment=document.getElementById('order-payment')?.value||'cash',total=calculateTotal(service,qty,unit),id=newOrderId(),order={id,name,phone,service,qty,unit,delivery,address,pickupTime,total,notes,payment};if(!total){alert(unit!=='Kg'?'Tarif per Pcs belum tersedia untuk layanan ini. Silakan gunakan Kg.':'Harga layanan belum tersedia. Silakan pilih layanan yang memiliki tarif.');return}orderSubmitting=true;if(button){button.disabled=true;button.dataset.originalText=button.textContent;button.textContent='Mengirim order...';button.classList.add('opacity-60')}try{const created=await createDbOrder(order),createdId=created.external_id||id;document.getElementById('track-id').value=createdId;document.getElementById('track-token').value=created.tracking_token;document.getElementById('track-link').href=`track.html?id=${encodeURIComponent(customerTenantId)}&order=${encodeURIComponent(createdId)}&token=${encodeURIComponent(created.tracking_token)}`;document.getElementById('track-link').classList.remove('hidden');showOrderSuccess(createdId,created.tracking_token);alert(`✅ Order ${createdId} berhasil dikirim. Kode pelacakan sudah diisi otomatis. Tidak perlu konfirmasi WhatsApp.`);form.reset();document.getElementById('track-id').value=createdId;document.getElementById('track-token').value=created.tracking_token;togglePickupFields();updateCustomerTotal()}catch(err){console.error(err);const msg=String(err?.message||'');alert(msg.includes('SUBSCRIPTION_EXPIRED')?'Masa layanan laundry sudah berakhir. Silakan perpanjang paket.':msg.includes('SERVICE_NOT_AVAILABLE')?'Layanan yang dipilih tidak tersedia saat ini.':msg.includes('ORDER_ID_EXISTS')?'Nomor order sudah digunakan. Silakan coba lagi.':'Order belum terkirim ke server. Silakan coba lagi')}finally{orderSubmitting=false;if(button){button.disabled=false;button.textContent=button.dataset.originalText||'🚀 Kirim Order';button.classList.remove('opacity-60')}}}
function normalizePhone(v){let p=v.replace(/\D/g,'');if(p.startsWith('0'))p='62'+p.slice(1);if(!p.startsWith('62'))p='62'+p;return p}function selectedDelivery(){return document.querySelector('input[name="delivery"]:checked')?.value||'pickup'}function togglePickupFields(){const x=selectedDelivery()==='pickup';document.getElementById('pickup-fields').classList.toggle('hidden',!x);document.getElementById('order-address').required=x}
function updateCustomerTotal(){const service=document.getElementById('order-service')?.value,qty=Number(document.getElementById('order-qty')?.value||0),unit=document.getElementById('order-unit')?.value||'Kg',price=Number((activeConfig.pricelist||{})[service]||0),box=document.getElementById('customer-total-preview');if(!box)return;if(!service||!qty||!price){box.classList.add('hidden');return}if(unit!=='Kg'){document.getElementById('customer-total-value').textContent='Rp 0';document.getElementById('customer-total-formula').textContent='Harga per Pcs belum tersedia. Silakan pilih Kg.';box.classList.remove('hidden');return}const total=Math.round(qty*price);document.getElementById('customer-total-value').textContent=`Rp ${total.toLocaleString('id-ID')}`;document.getElementById('customer-total-formula').textContent=`${qty} Kg × Rp ${price.toLocaleString('id-ID')} = TOTAL Rp ${total.toLocaleString('id-ID')}`;box.classList.remove('hidden')}
function quickAsk(type){document.getElementById('chat-input').value=type;sendMessage()}function addBubble(t,u=false){const b=document.createElement('div');b.className='chat-bubble '+(u?'bg-blue-600 text-white max-w-[85%] ml-auto':'bg-slate-100 dark:bg-slate-700 max-w-[92%]');b.innerText=t;const box=document.getElementById('chat-box');box.appendChild(b);box.scrollTop=box.scrollHeight}
document.addEventListener('DOMContentLoaded',()=>{document.querySelectorAll('input[name="delivery"]').forEach(x=>x.addEventListener('change',togglePickupFields));document.getElementById('chat-input').addEventListener('keydown',e=>{if(e.key==='Enter'){e.preventDefault();sendMessage()}});document.getElementById('order-service').addEventListener('change',updateCustomerTotal);document.getElementById('order-qty').addEventListener('input',updateCustomerTotal);document.getElementById('order-unit').addEventListener('change',updateCustomerTotal);ensureTrackingTokenField();togglePickupFields()});document.addEventListener('jfs:portal-config-ready',()=>{if(typeof loadTenantPortal==='function')loadTenantPortal()});document.addEventListener('DOMContentLoaded',async()=>{const header=document.getElementById('display-store-name'),address=document.getElementById('display-store-address');if(header)header.textContent='JFS Laundry AI';if(address)address.textContent='Memuat data usaha...';ensurePaymentField();window.sendOrder=sendOrder;window.trackOrder=trackOrder;try{await syncPortalConfig();if(typeof loadTenantPortal==='function')loadTenantPortal()}catch(e){console.warn('Portal config sync failed',e)}});
